variables:
  headers:
    none: ''
    plaintext: "--header Accept:text/plain,text/html;q=0.9,application/xhtml+xml;q=0.9,application/xml;q=0.8,*/*;q=0.7 --header Connection:keep-alive"
    connectionclose: "--header Connection:close"
  presetHeaders: none

jobs:
  wrk:
    source:
      repository: https://github.com/aspnet/benchmarks.git
      branchOrCommit: master
      project: src/WrkClient/WrkClient.csproj
    readyStateText: 'Running '
    sdkVersion: 3.1.102
    aspNetCoreVersion: '3.1'
    runtimeVersion: '3.1'
    waitForExit: true
    variables:
      connections: 256  # total number of HTTP connections to keep open with each thread handling N = connections/threads
      duration: 15 # duration in seconds
      threads: 32 # total number of threads to use
      pipeline: 1
    requiredVariables:
      serverUri: http://10.0.0.102
      serverPort: 5000
    arguments: "-c {{connections}} {{serverUri}}:{{serverPort}}{{path}} --latency -d {{duration}}s -t {{threads}} {{headers[presetHeaders]}} {% if pipeline > 1 %} -s scripts/pipeline.lua -- {{ pipeline }} {% endif %}"
